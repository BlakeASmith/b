---
- name: Dev Setup
  hosts: all

  tasks:
    - name: include roles for debian
      block:
        - name: include bash
          include_role:
            name: bash
        - name: include vim
          include_role:
            name: vim
        - name: include brew
          include_role:
            name: linuxbrew
        - name: install lazygit
          homebrew:
            name: lazygit
            state: present
      when: ansible_facts['os_family']|lower == 'debian'
